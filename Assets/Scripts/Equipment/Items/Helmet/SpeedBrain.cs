using UnityEngine;

[CreateAssetMenu(fileName = "Data", menuName = "Data/SpeedBrain", order = 5)]
public class SpeedBrain : Helmet
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
