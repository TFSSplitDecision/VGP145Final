using UnityEngine;

[CreateAssetMenu(fileName = "Data", menuName = "Data/TankBrain", order = 6)]
public class TankBrain : Helmet
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
